# Planned Final-Version Updates

The initial poster draft is representative of the final submission direction. If accepted, we plan the following updates before the final poster upload:

1. Add a larger selected validation run for the recommended `256 x 256` and `512 x 512` tiling settings.
2. Add more visual QA panels across several drone scenes, not only the current representative image.
3. Add downstream disaster-analysis checks that connect reconstruction quality to visual inspection or damage-assessment tasks.
4. Refine the poster layout for print after committee feedback, while keeping the same central claim: tiling is the practical systems control for CDC on full-resolution drone imagery.

